# Amber & Forest — дизайн-система

Переносимая дизайн-система «сайт как фильм»: тёмная сцена и тёплая бумага, один янтарный акцент, лесной зелёный как драгоценный материал, крупная дисплейная типографика, моношрифт для разметки, кинематографичное движение. Эталонная реализация — сайт геотехнической лаборатории МОСТДОРГЕОТРЕСТ (Astro + GSAP/ScrollTrigger + Lenis), но система не привязана к предметной области: положите этот файл в любой проект — и собирайте по нему.

**Как пользоваться.** Раздел 2 — токены, копируются в `:root` как есть. Разделы 3–5 — правила цвета, шрифта и ритма, их не обсуждают, им следуют. Раздел 6 — общие компоненты, раздел 7 — модули-секции с описанием назначения, устройства, движения и мобильного поведения: из них собирается любая страница. Раздел 10 — как адаптировать систему под другой бренд, не сломав её.

---

## 1. Философия

**Страница — это фильм.** Главная — не набор блоков, а последовательность актов: пролог (прелоадер), акт 01 (герой с объектом на сцене), акт 02 (погружение — пиновая сцена-путешествие), акты 03–06 (галереи, доверие, прямой эфир, досье), финал (контакты с крупной фразой и одной главной кнопкой). У каждого акта есть ярлык-eyebrow «Акт 0N · Название» и своя «температура». Подстраницы — главы: короткая шапка `PageHead` и один-два модуля.

**Четыре материала, а не цвета.** *Навь* — тёмная сине-графитовая сцена (всё, что «внутри» и «ночью»). *Крем* — тёплая бумага (всё документальное, дневное). *Янтарь* — интерфейс: всё, что кликается, ведёт и считает. *Лес* — драгоценная поверхность: глубокий зелёный появляется редко, только на нескольких «дорогих» карточках и как семантика «действует». Материалы не смешиваются: зелёный не бывает кнопкой, янтарь не бывает фоном большой плоскости.

**Один интерфейсный акцент.** Янтарь — единственный цвет навигации, кнопок, ярлыков, стрелок, ховеров, цен и метрик. Второй цвет системы работает как материал поверхности и как статус, но не как «ещё одна кнопка». Это делает интерфейс читаемым: если что-то янтарное — с этим можно взаимодействовать или это число.

**Тёплые нейтрали.** Ни чистого белого, ни чистого чёрного, ни серых теней. Бумага — кремовая, чернила — тёплый чёрный, тени — коричневые, линии — прозрачные чернила. Белый допустим только как «бумага оригинала» под сканом или логотипом.

**Свет обязателен.** У каждой тёмной поверхности есть inset-блик сверху, свечение-круг в углу и, у самых важных, зерно. У каждой CTA — цветная тень. Плоских тёмных прямоугольников нет.

**Документность.** Сайт говорит языком лаборатории: моношрифт для реквизитов, номеров, координат, статусов; ранги «01 · IEEE»; счётчики «03 / 10»; пунктирные линии как в бланке; сканы реальных документов на «мате» с миллиметровкой. Данные не прячутся — они и есть декор.

**Движение — камера, а не эффекты.** Скролл управляет камерой: пин и путешествие сквозь слои, горизонтальная «долли»-галерея, параллакс внутри кадра, наклон карточек за курсором. Появления одноразовые и надёжные; на тач-устройствах — через IntersectionObserver; при `prefers-reduced-motion` всё статично.

**Предметные цвета неприкосновенны.** Цвета слоёв грунта, соцсетей, статусов и live-индикаторов — это смысл, а не стиль. Их не перекрашивают под акцент.

---

## 2. Токены (копировать в `:root`)

```css
:root{
  /* навь — тёмная сцена */
  --bg:#111826; --bg-2:#141B2A;
  --ink:#F4EFE4; --ink-dim:#C9C2B2; --muted:#948E80;
  --line:rgba(244,239,228,.13); --line-soft:rgba(244,239,228,.06);

  /* крем — бумага */
  --paper:#F2ECDF; --paper-2:#E9E1CF; --paper-card:#FBF8F1;
  --ink-d:#221A0E; --ink-d-dim:rgba(34,26,14,.68); --mut-d:rgba(34,26,14,.52);
  --line-d:rgba(34,26,14,.16); --line-d-soft:rgba(34,26,14,.09);

  /* янтарь — интерфейс */
  --amber:#E8A33D; --amber-hi:#FFC96B; --amber-d:#A66A14; --amber-deep:#8f5a10;

  /* лес — драгоценная поверхность и статус */
  --emerald:#5DB585; --emerald-hi:#A6D9BE; --emerald-d:#0A6E36; --emerald-rgb:93,181,133;

  /* шрифты */
  --font-d:'Unbounded',system-ui,sans-serif;        /* дисплей */
  --font-s:'Inter',system-ui,sans-serif;            /* текст */
  --font-m:'JetBrains Mono',ui-monospace,monospace; /* разметка */

  /* ритм */
  --ease:cubic-bezier(.22,1,.36,1);
  --wrap:min(1280px,92vw);
}
```

Производные величины (не токены, но константы системы):

```
Радиусы:      пилюля 99px · кнопка-иконка 9–13px · чип 99px · карточка 18–24px · панель/модалка 24–26px
Тени бумаги:  покой 0 2px 10–16px rgba(60,42,12,.05–.06) · ховер 0 14–18px 30–42px rgba(60,42,12,.12–.14)
Тени нави:    панель 0 30px 70px rgba(30,24,10,.28) · модалка 0 44px 110px rgba(0,0,0,.45–.5)
Свечение CTA: 0 10px 34px rgba(232,163,61,.3)
Inset-блик:   inset 0 1px 0 rgba(255,255,255,.08)
Скрим:        rgba(14,11,7,.62–.72) + backdrop-filter blur(10px)
Зерно:        SVG feTurbulence baseFrequency .55–.9, opacity .22–.3, mix-blend-mode overlay
Секция:       padding clamp(90px,14vh,150px) 0 · шапка секции margin-bottom clamp(34px,6vh,60px)
Сетки:        gap 14px (карточки), 16px (крупные карты), 10px (леджер/чипы 6–8px)
```

---

## 3. Цвет

### 3.1 Роли

| Токен | На чём | Для чего | Контраст |
|---|---|---|---|
| `--amber` | навь | активный пункт меню, ярлыки секций, цифры-метрики, стрелки, live-«подключение», кнопки | 8.2:1 |
| `--amber-hi` | — | светлая ступень градиента кнопок и крупных чисел | — |
| `--amber-d` | бумага | ярлыки, кикеры, цены, индексы, ссылки, иконки, ховер-заголовки | 4.9:1 |
| `--amber-deep` | — | скроллбар, глубокие детали | — |
| `--emerald-d` | бумага | ярлыки/кикеры/ссылки/статусы **только внутри лесных разделов**; точка статуса «действует» | 5.4:1 |
| `--emerald` | лесная поверхность | делитель-волосок, рамки, свечение (через `rgba(var(--emerald-rgb),α)`) | 7.1:1 на нави |
| `--emerald-hi` | лесная поверхность | подписи, ссылки, линии графов | 11:1 |

### 3.2 Готовые смеси

```
Кнопка primary:        linear-gradient(135deg,var(--amber-hi),var(--amber)); текст #1d1302
Янтарная поверхность:  linear-gradient(145deg,#E9A641,#CF8218); текст #231D14; вторичный rgba(35,29,20,.62–.74)
Ховер-рамка (бумага):  rgba(166,106,20,.45–.5)          Янтарный wash: rgba(232,163,61,.08–.2)
Лесная CTA-карта:      linear-gradient(160deg,#123420 0%,#0A1D12 70%)
Лесная лид-карта:      linear-gradient(150deg,#14472B 0%,#0F3320 48%,#0A1D12 100%)
Обвязка леса:          рамка 1px rgba(var(--emerald-rgb),.22–.24) · свечение-круг radial rgba(var(--emerald-rgb),.28–.32)→transparent 65%, blur 12–14px, правый верхний угол · inset-блик · тень 0 30px 70px rgba(10,40,24,.22–.3)
Лесная ховер-рамка:    rgba(10,110,54,.5–.55)           Лесной wash: rgba(10,110,54,.06–.12)
Тёмная панель нави:    linear-gradient(135deg,#151D2C,#0E141F 62%) + wash radial rgba(232,163,61,.2) в правом верхнем углу
Стекло на нави:        rgba(255,255,255,.045), рамка rgba(255,255,255,.12); ховер .07 и рамка rgba(232,163,61,.55)
Светлая pending-карта: linear-gradient(145deg,#F8F5ED,#EEE8DC), рамка пунктир, водяной знак rgba(10,110,54,.07)
Градиент финального H2: linear-gradient(180deg,var(--ink-d) 35%,var(--emerald-d) 125%) + background-clip:text
Мат под скан:          linear-gradient(180deg,#E4DCCB,#EDE6D7) + миллиметровка rgba(34,26,14,.045–.05) шаг 22px
Сцена просмотрщика:    #DCD3C0 + radial rgba(255,255,255,.45) сверху
```

### 3.3 Семантика и предметные цвета (не перекрашивать)

```
Статус:      действует #0A6E36 · истекает (<90 дн.) #A66A14 + пульс · бессрочно/справочно --mut-d @.6 · истёк #B33A3A (текст #9B2C2C)
Live:        точка «в эфире» #41D97C с glow — единственный яркий зелёный, только 6–7px
Соцсети:     Telegram #7CC7EE, подложка rgba(42,171,238,.28→.06)
Предметная стратиграфия (слои грунта): #6F563C · #A26A35 · #DDB56A · #B9D4E4 · #8E9198 · #47836E
Брендовая лента: фон #2F5A4A, контурные слова кремом (stroke rgba(244,239,228,.4)), слова-вставки --amber
```

### 3.4 Правила

- Зелёный никогда не интерфейс: нет зелёных кнопок, пунктов меню, ярлыков секций, ховер-рамок вне лесных разделов.
- Неоновые оттенки (яркость > 60% при насыщенности > 60%) запрещены везде, кроме 6-пиксельной live-точки.
- Оба акцента на одном элементе — только как янтарные цифры/кнопка **внутри** лесной поверхности.
- Пропорции по площади: ~84% поверхности, ~12% янтарь, ~3% лес, ~1% семантика.
- Контраст: текст ≥ 4.5:1, крупный текст и UI ≥ 3:1 — проверять каждую новую пару.

---

## 4. Типографика

| Роль | Шрифт | Начертания | Применение |
|---|---|---|---|
| Дисплей | **Unbounded** | 500–800 | H1–H2, заголовки карточек, крупные числа, цены, название бренда |
| Текст | **Inter** | 300–600 | абзацы 13.5–16px / 1.55–1.7, названия в списках, кнопки |
| Разметка | **JetBrains Mono** | 400–500 | ярлыки, кикеры, реквизиты, статусы, счётчики, координаты, теги 9–12px |

Паттерны:
- **Eyebrow секции** — mono `clamp(10px,1vw,12px)`, UPPERCASE, `letter-spacing:.4em`; формат «Акт 0N · Название» / «Финал · …»; цвет `--amber` на нави, `--amber-d` на бумаге (в лесных разделах допускается `--emerald-d`).
- **H1** — `clamp(32px,min(7vw,10.5vh),104px)` 800, `line-height 1.02`, одно слово-акцент цветом `--amber`.
- **H2 секции** — `clamp(28px,4.6vw,64px)` 800, `line-height 1.05`; тяжёлые шапки ужимать до `clamp(28px,4.2vw,58px)` и `max-width` 14–16ch.
- **Заголовок карточки** — Unbounded 600 `clamp(18–21px, 1.7–2vw, 24–29px)`, `letter-spacing −.015em`; лид-карта — 500 `clamp(27px,3vw,46px)`, `−.025em`.
- **Кикер карточки** — mono 10px UPPERCASE `.12–.16em`.
- **Ранг-чип** — mono 10px в пилюле `1px currentColor`, `opacity .75`: «01 · IEEE», «02 · Plaxis».
- **Реквизиты** — mono 10–11px, `letter-spacing .04em`, `word-break:break-all`, `font-variant-numeric:tabular-nums` (все цифры системы — табличные).
- **Крупные числа** — Unbounded 600 `clamp(22px,2.2vw,30px)`; цвет по роли: янтарь — метрика, чернила — количество, лес — только «действует».
- Тексты на бумаге не шире 52–62ch; на тёмном — 44–72ch.

---

## 5. Сетка и ритм

- Контейнер `--wrap: min(1280px,92vw)`, центрирован.
- Вертикальный ритм: секция `clamp(90px,14vh,150px)`, тяжёлые финальные — до `17vh`; шапка секции отделена `clamp(34px,6vh,60px)`.
- Сетки карточек: 4 колонки ≥1180px, 3 до 1180, 2 до 820, 1 до 480 (документы); 2 колонки (публикации, продукты, сервисы) → 1 на ≤680–900px; бенто `repeat(4,1fr) dense` с широкими флагманами `span 2`.
- Featured-раскладка «лид + боковая колонка»: `minmax(0,1.45fr) minmax(320px,.75fr)`, боковая — две равные карты по вертикали; ≤900px — колонки складываются, боковые карты идут в ряд; ≤680px — всё в одну колонку.
- Шапка «заголовок слева, интро/леджер справа»: `minmax(0,1.1–1.25fr) minmax(280–300px,.65–.75fr)`, выровнено по низу.
- Радиусы уменьшаются на мобильных на 4–6px; модалки становятся полноэкранными без радиуса.

---

## 6. Общие компоненты

**Кнопка primary** — пилюля, `padding 15px 28px` (`.btn-sm` 12px 24px), градиент `amber-hi → amber`, текст `#1d1302` 600, свечение. Ховер: снизу вверх заливается кремом (`.btn-fill` translateY 101% → 0, .45s). Магнитный сдвиг за курсором ±18%/32% на десктопе.

**Кнопка ghost** — прозрачная с рамкой `--line` (на бумаге `--line-d`, цвет `--ink-d`; в модалках всегда тёмный вариант). Ховер: заливка янтарём, текст темнеет.

**Кнопка-иконка** — 30–42px, радиус 9–13px, рамка `--line-d`; ховер — рамка `--amber-d`, подложка `rgba(232,163,61,.1)`.

**Карточка на бумаге** — `--paper-card`, радиус 18–24px, рамка `--line-d-soft`, тень покоя; ховер: подъём −4…−5px, янтарная рамка, глубокая тень, заголовок → `--amber-d`. Статичная (без ссылки) — пунктирная рамка, без ховера.

**Карточка-стекло на нави** — см. 3.2; тег `--amber`, чипы `rgba(255,255,255,.16)`, примечание на пунктире `rgba(255,255,255,.14)`.

**Лесная карта** — см. 3.2 «обвязка леса»; текст крем `rgba(242,240,230,.72–.78)`, подписи `.55`, ссылки `--emerald-hi`, янтарные числа и кнопки внутри допустимы.

**Ярлык-eyebrow, кикер, ранг-чип** — см. раздел 4.

**Чип/тег** — mono 9–11px пилюля `1px --line-d` (на нави `rgba(255,255,255,.16)`), `padding 5–8px 9–14px`; статусный чип «Скоро» — янтарная рамка `.6` и янтарный текст.

**Табы-фильтр** — mono 11.5px пилюли `.06em`; активный — заливка `--ink-d`, текст `--paper`, счётчик `<em>` `--amber`; ховер — рамка `--amber-d`. Фильтрация: скрытые `display:none`, показанные получают анимацию `pop` (opacity 0→1, y 18→0, scale .985→1, .5s, задержка `k·45ms`).

**Леджер (стат-плитки)** — сетка 2×2 или 4×1 карточек на бумаге, число Unbounded + подпись mono UPPERCASE 9.5px.

**Подзаголовок-линейка** — mono 10px UPPERCASE, `border-bottom 1px --line-d`, слева тема, справа счётчик «11 материалов».

**Строки на пунктире** (реквизиты, РИНЦ, факты в просмотрщике) — `grid`, `padding 7–16px 0`, `border-bottom 1px dashed --line-d(-soft)`; ховер строки — сдвиг padding и цветной wash.

**Бейджи на изображении** — mono 9.5px UPPERCASE в пилюле с blur: тёмный `--ink-d`/`--amber-d`/`--emerald-d` с текстом `--paper`; светлый `rgba(251,248,241,.78)` с рамкой.

**Точка статуса** — 7px круг + `box-shadow 0 0 0 3–4px` того же цвета `@.12`; «истекает» и «pending» пульсируют (`pulse` 2–2.4s).

**Модальное окно** — фиксированный слой z 3000, скрим, карта `--paper` радиус 24px, шапка на `--paper-card` с eyebrow + H3 + действия, кнопка закрытия 42px. Вход: скрим .35s, карта `y 40–46 → 0, scale .96 → 1` expo.out .55s; выход .28s. Блокирует overflow и Lenis, возвращает фокус, закрывается Esc и кликом по скриму. ≤640px — на весь экран.

**Просмотрщик документа** — модалка `min(1180px,100%) × min(820px,94vh)`, сетка «сцена 1.5fr / панель minmax(300px,.7fr)». Сцена: мат `#DCD3C0`, изображение по центру с тенью `0 24px 60px rgba(34,26,14,.32)`, вход `opacity 0→1, y 8→0`, листание `x ±26`; круглые стрелки 46px `rgba(251,248,241,.9)`+blur; спиннер 34px с янтарной дугой. Панель: счётчик, чип владельца, кикер, заголовок, орган, факты на пунктире, внизу primary «Открыть» + ghost «Скачать», подсказка про клавиши. Управление: ← → , Esc, свайп, предзагрузка соседей. ≤900px — полноэкранный, сцена сверху, панель снизу `max-height 46vh`, стрелки в нижних углах сцены.

**Прелоадер** — тёмная сцена, имя бренда, набегающий процент (Unbounded 300 `clamp(60px,12vw,160px)` `--amber`), глифы-«шум» `rgba(232,163,61,.5)` до реальной загрузки; уходит по `load` с фолбэком 8 с; при `reduced-motion` — сразу.

**HUD** — фиксированный моношрифтовый прибор (например, глубиномер `-48.0 м` + рельс прогресса) в правом нижнем углу, `--amber` на нави / `--amber-d` на бумаге, на телефоне — только цифра с тенью.

**Курсор** — янтарная точка 8px + кольцо 24px; над ссылками кольцо 64px `rgba(232,163,61,.16)`; над перетаскиваемым — 92px с подписью «Дальше». Скрыт на тач и ≤900px.

---

## 7. Модули-секции

Каждый модуль — самостоятельный `.astro`-компонент с собственным CSS-блоком в `global.css` (`/* ---------- ИМЯ ---------- */`) и, если нужно, скриптом в `main.js` или инлайн-`<script>`. Описание: назначение → устройство → цвет → движение → мобильное поведение.

### 7.1 Nav (навигация)
Фиксированная шапка: слева бренд (иконка 36px + имя Unbounded + подпись mono), по центру 5–6 пунктов (пилюли, активный `--amber` с подчерком, ховер-подчерк из правого края в левый), справа акцент-пилюля (3D-тур/ключевое действие, янтарный градиент), ghost-пилюля с телефоном и бургер. При скролле > 40px — фон `rgba(11,15,22,.66)`/`rgba(242,236,223,.75)` + blur, тонкая линия снизу. На бумаге (`body.on-light`) все цвета переключаются на тёмные. Мобильное меню: полноэкранный крем с blur, пункты Unbounded `clamp(24px,7vw,40px)` с mono-номерами `01…`.

### 7.2 Loader (пролог) — см. «Прелоадер» в 6.

### 7.3 Hero (акт 01)
Полноэкранная навь с объектом-сценой справа (WebGL-планета/любой 3D-объект или видео), звёздное поле, зерно и виньетка. Слева: тег с live-точкой и mono-строкой доверия, H1 с одним янтарным словом, абзац 44ch, primary-кнопка, три метрики (число `--amber` + mono-подпись). Внизу — стрелка «начать …» с капающей линией. Скролл пинит герой на ~3 экрана: объект приближается и «пробивается» сквозь поверхность, текст уходит вверх. На телефоне пина нет — объект зумится, пока герой уезжает.

### 7.4 Descent (акт 02, сцена-путешествие)
Пиновая «шахта» на N экранов: слои-горизонты с предметными цветами, у каждого — ярлык глубины mono, тип, H3, абзац, чипы, огромный контурный индекс `01…06` (`-webkit-text-stroke`), частицы и живые микроанимации, характерные слою (корни, галька, дрожь, лёд, трещины, пузыри). HUD-глубиномер интерполирует метры по прогрессу. Шапка акта появляется на первом слое и уходит. На телефоне слои становятся обычными блоками `padding 84px 0 90px`, глубиномер считается по проходу каждого блока.

### 7.5 Strip (брендовая лента)
Бегущая строка на всю ширину: фон `#2F5A4A` (или `--paper-2` для светлой под-ленты), слова Unbounded 800 `clamp(34px,6vw,84px)` — контурные кремом, каждое второе-третье залито янтарём. Скорость реагирует на скорость скролла, направление чередуется. На ≤640px — `clamp(24px,8vw,40px)`.

### 7.6 Objects (акт 03, горизонтальная долли-галерея)
Светлая пиновая секция: слева шапка (eyebrow, H2, абзац, стрелка «двигаемся вдоль» с анимированной линией, счётчик `05 / 08` и прогресс-бар), далее ряд панелей `min(62vw,860px) × 72vh` с фото, огромным контурным индексом в углу, подписью снизу (mono-тег `--amber-hi`, H3 крем, подпись). Скролл едет по горизонтали; панели чуть скашиваются по скорости, фото внутри двигаются с параллаксом. Вход в секцию включает «свет» (`body.on-light`). На телефоне — вертикальный список карточек с теми же подписями.

### 7.7 Clients (акт 04, логотипы)
Два ряда белых чипов 176×92 с логотипами `object-fit:contain`, ряды едут навстречу друг другу, ховер — подъём и янтарная рамка.

### 7.8 Live (акт 05, прямой эфир)
Сетка 3×2 (`→ 1` на ≤640px) тёмных карточек-мониторов 16:9 с видео (HLS, ленивая загрузка), контурный индекс `01…`, плашка-статус с точкой (`подключение` янтарь / `в эфире` #41D97C / `офлайн` серый) и mono-меткой `CAM 0N`, подпись H3. Недоступная камера показывает иконку и текст «Камера недоступна».

### 7.9 Docs (акт 06, досье)
См. «Карточка документа» и «Просмотрщик» ниже. Секция на `--paper-2` с янтарным wash в правом верхнем углу; шапка «H2 слева / леджер 2×2 справа»; панель табов-фильтров и легенда статусов; сетка карточек 4/3/2/1; примечание с mono-номером.
**Карточка документа**: верх — «мат» с миллиметровкой и сканом первой страницы, прижатым к низу (портрет: `height 244px, object-fit cover top`; альбом: `height auto`), скруглён сверху 6px, тень; бейджи владельца и «N стр.»; 3D-наклон за курсором `rotateX(−y·7°) rotateY(x·9°)` от нижней кромки с бликом `radial 260×180 rgba(255,255,255,.35)`. Низ — кикер, заголовок Inter 600 15px, орган, реквизиты mono, подвал на пунктире: строка статуса, ниже «СМОТРЕТЬ ↗» и кнопка-скачивание с размером файла. Истёкший — скан `grayscale(.5)`. Вся карточка — `role=button`, Enter/Space открывают просмотрщик, скачивание не открывает его (`data-stop`). Сроки считаются на клиенте.

### 7.10 Contacts (финал)
Крем, водяной знак (контурное огромное слово), eyebrow «Финал · …», H2 с градиентом чернила→лес, абзац. Сетка `1.15fr / 1fr`: слева **лесная CTA-карта** (лейбл `--emerald-hi`, телефон Unbounded, волосок-делитель, абзац, primary «Написать», ghost-кнопки крем, mono-координаты `--amber` внизу, вертикальная стратиграфическая полоса справа, зерно), справа три инфо-карточки «Телефоны / Почта / Адрес» с шапкой «ЛЕЙБЛ … 01» (лейбл `--emerald-d`, индекс `--amber-d`, линия `rgba(10,110,54,.26)`), строки с янтарными иконками, ссылка на карту. Ниже стратиграфическая полоса 6px и футер на линии: копирайт, мини-навигация, mono-реквизиты `--amber-d`. На подстраницах — тот же модуль с eyebrow «Контакты».

### 7.11 PageHead (шапка подстраницы)
Крем: ссылка «← На главную», eyebrow `--amber-d`, H1, подзаголовок 62ch. Никаких иллюстраций — подстраница начинается с текста.

### 7.12 Equipment (бенто-парк с фильтрами)
Витрина «100+» (счётчик Unbounded с набеганием + подпись + абзац), табы-фильтры с счётчиками, факты парка. Сетка `repeat(4,1fr) dense`: карточка — белая плитка-фото 150px, mono-индекс в углу, кикер, H3, характеристики; флагманы `span 2` в формате «паспорт» (фото крупнее, список характеристик в две колонки, чип «ФЛАГМАН»); CTA-плитка (3D-тур) — тёмная. Фильтр — pop-анимация.

### 7.13 Tests (испытания с графиками и протоколами)
Сетка карточек `minmax(255px,1fr)`: mono-номер, H3, описание, SVG-график кривой испытания, который дорисовывается (`stroke-dashoffset`) при появлении (`.is-in`), ряд скачиваний «Протоколы» — чипы-кнопки PDF с иконкой. Ховер — янтарный radial-wash, следующий за курсором (`--gx`).

### 7.14 Calc (калькулятор/заявка)
Две колонки формы и итога на бумаге; поля с рамкой `--line-d`, фокус — янтарная рамка; итог — крупное число Unbounded `--amber-d`. ≤900px — одна колонка.

### 7.15 Product panel (тёмная панель продуктовой серии)
Панель на нави внутри светлой страницы (радиус 26px, wash, созвездие точек-линий в углу — скрыто ≤1180px). Шапка: эмблема + имя Unbounded 800 с трекингом `.06em` + подпись mono `--amber`; абзац 72ch; сетка 2×2 одинаковых стеклянных карточек: иконка 44px (предметная), тег «СЕРИЯ · 0N» `--amber`, H3, абзац, чипы, примечание на пунктире. Все карточки одного цвета — разноцветить нельзя.

### 7.16 Services (сервисы)
Две карточки на бумаге с логотипом-плиткой 52px (белая, рамка), H3, абзац, mono-бейдж технологии внизу; карточка-ссылка — бейдж со стрелкой, уезжающей вправо.

### 7.17 Papers (семейство «наука»: лид + боковые + сетка)
Эталон для любых «витрин достижений» (публикации, курсы, кейсы, награды). Каждый элемент — полноценная карточка с метаданными; списков-«заглушек» без описаний в системе нет.
- **Шапка** «H2 / интро справа», **линейка** с счётчиком.
- **Лид-карта** (лесная): ранг-чип, визуал 245px (сетка `rgba(255,255,255,.07)`, линии графа `--emerald-hi` 1.5px с glow, янтарные точки со stroke цвета фона и пульсом, чип-метрика `rgba(10,29,18,.72)`+blur с числом Unbounded 26px `--amber`), кикер `--emerald-hi`, заголовок 500 `clamp(27px,3vw,46px)`, текст, авторы, ссылка `--emerald-hi`; `min-height 620–650px`.
- **Боковая колонка**: янтарная карта (тёмный текст, ранг, кикер, заголовок 600, текст, авторы, цена/ссылка) и pending-карта (светлая, пунктир, водяной знак, статус-строка с пульсирующей точкой).
- **Сетка элементов** 2 колонки (последний нечётный — на всю ширину): верхняя строка mono «ранг · год», кикер `--emerald-d`, заголовок 600 `clamp(21px,2vw,29px)` (ховер → `--emerald-d`), текст, источник `--emerald-d`, авторы, теги, стрелка в углу; ховер — подъём и **полоска 3px сверху** `linear-gradient(90deg,var(--emerald-d),var(--emerald))` `scaleX 0→1`. Статичные — пунктир, подпись «Архивная запись».

### 7.18 Courses (курсы = семейство Papers)
Тот же набор: линейка «Программы · год / 03 курса», лесной лид (визуал — граф-нейросеть, метрика «36 · академических часов»), кикер с датой старта и форматом, текст, авторы, подвал с ценой Unbounded `--amber` и двумя ссылками — «Записаться» и пилюля-документ «Программа · PDF» (рамка `rgba(166,217,190,.35)`, ховер `rgba(var(--emerald-rgb),.14)`); янтарная карта второго курса с ценой отдельной строкой; pending-карта партнёрской программы с водяным знаком; примечание об удостоверении.

### 7.19 Price modal (прайс)
Модалка с iframe таблицы: шапка с eyebrow даты, действия «XLS»/«PDF» (кнопки-иконки с подписью), спиннер до загрузки (макс. 12 с), подвал с запасными ссылками «Открыть в новой вкладке» / «PDF» (подпись показывается только ≤640px). Тело `overflow:auto` для iOS.

---

## 8. Движение

- **Easing** — `--ease: cubic-bezier(.22,1,.36,1)` для UI; `power2/3.out` для появлений; `expo.out` для модалок; `sine.inOut` для дыхания; `steps(6)` для «механических» эффектов.
- **Появление карточек** — `y 34 → 0, opacity 0 → 1`, .6s, задержка `(k%4)·.06s`, один раз, триггер `top 94%`, после — `clearProps`. Шапки секций — дети `y 40`, stagger .1. Десктоп — GSAP ScrollTrigger; **тач** — класс `.rv` (opacity 0, `translateY(26px)`, переход .55s) + IntersectionObserver (`.rv-in`) + страховочные проверки на scroll/load/pageshow/orientationchange и принудительный показ всего, что выше экрана, через 8 с. `ScrollTrigger.config({ignoreMobileResize:true})`.
- **Ховеры** — .35–.4s: подъём −3…−5px, рамка, тень; стрелки ссылок `translate(3px,−3px)`; сканы — наклон .25s ease-out.
- **Пин и камера** — герой ~3.3 экрана, шахта N экранов, галерея по горизонтали с velocity-skew; всё через `scrub`.
- **Плавный скролл** — Lenis `lerp .085` на десктопе, нативный на тач.
- **Пульс** — `@keyframes pulse {0%,100%{opacity:1}50%{opacity:.3}}` для live, статусов, точек графов.
- **Фильтры** — `pop` .5s с каскадом 45ms.
- **`prefers-reduced-motion`** — прелоадер скрыт, пины сняты, появления и наклоны выключены, модалки без tween, графики нарисованы сразу.

---

## 9. Адаптив, доступность, производительность

- Контрольные точки: 1280 (сетки 4→3), 1180 (созвездия/декор скрываются), 900 (пины сняты, курсор и рельс скрыты, featured складывается, модалки полноэкранные), 820/768 (2 колонки), 680 (1 колонка семейства papers), 640 (лента мельче, live в 1 колонку, прайс на весь экран), 480 (документы в 1 колонку), 390 — эталонный телефон.
- `html{overflow-x:clip}` — горизонтального скролла нет никогда; всё широкое (ленты, свечения, частицы) живёт в `overflow:hidden`-контейнерах.
- Фокус: `:focus-visible` — кольцо `--amber` 2px с отступом 3px; карточки-кнопки имеют `tabindex=0`, `role=button`, `aria-label`; модалки — `role=dialog`, `aria-modal`, возвращают фокус.
- Skip-link, `aria-hidden` на декоре, `alt` на сканах и фото, `aria-pressed` на табах.
- Шрифты — локальные WOFF2 с `preload` трёх ключевых; изображения — `loading=lazy`, `decoding=async`, размеры заданы; превью документов — WebP 560/1200px; видео — `preload=none`; тяжёлые скрипты (HLS) — динамический импорт.
- Инлайн-стиль фона в `<head>` — цвет сцены появляется до CSS; `theme-color` совпадает с фоном страницы.
- `canonical`, `og:*` с абсолютными URL, `robots.txt` и `sitemap.xml`.

---

## 10. Адаптация под другой проект

1. **Сохраните структуру материалов**: тёмная сцена, тёплая бумага, один интерфейсный акцент, один «драгоценный» материал. Меняются значения, не роли.
2. **Смена акцента**: замените четыре `--amber*` на свою тёплую пару (яркий для тёмного ≥ 7:1, тёмный для бумаги ≥ 4.5:1) и пересчитайте `rgba(...)`-смеси (свечение, wash, ховер-рамки) от нового hex. Акцент должен оставаться тёплым и не неоновым.
3. **Смена материала**: замените `--emerald*` и два лесных градиента на глубокий, тёмный оттенок своего бренда (светлота ≤ 30% у основной ступени); мятную ступень `-hi` подберите с контрастом ≥ 10:1 на самой тёмной точке градиента.
4. **Нейтрали** можно сдвигать по температуре (холоднее/теплее), но не к чистым белому/чёрному и не к серым теням.
5. **Шрифты** заменяются по ролям: дисплей — широкий гротеск с характером (Unbounded → Syne, Space Grotesk), текст — нейтральный гротеск, разметка — моноширинный с кириллицей.
6. **Модули** берутся как паттерны: «сцена-путешествие» может быть про этажи, годы, слои продукта; «досье» — про сертификаты, лицензии, награды; «семейство papers» — про кейсы, публикации, курсы, релизы; «прямой эфир» — про любые живые данные. Названия актов и eyebrow-формат «Акт 0N · …» сохраняются — это голос системы.
7. **Не переносите** предметные цвета (стратиграфия, live-зелёный) как декор — замените их на семантику своей области или уберите.
8. Проверка после адаптации: контрасты всех новых пар, отсутствие неона, доля акцента ≤ 15% площади, `prefers-reduced-motion`, 390px без горизонтального скролла, появления карточек на тач.
